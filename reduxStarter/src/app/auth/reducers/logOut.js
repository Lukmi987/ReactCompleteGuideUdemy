import { LOG_USER_OUT } from "../../../constants/actionTypes";

const initialState = null;

//update just certain properties
export default (state = initialState) => (
    action.type === LOG_USER_OUT ?  : state
);


